class {{ class_name }}Plugin:
{% comment %} app plugins {% endcomment %}
    def __init__(self, plugin_name, printer, logger):
        self.__printer = printer
        self.__logger = logger
        self.__plugin_name = plugin_name

        self.__logger.debug("Initialized plugin")

    def run(self, name, foo="foo"):
        self.__printer.print(f"Hello {name} from plugin {self.__plugin_name} with arg foo: {foo}")
