from {{ app.name.package }} import ioc


class DefaultPlugin:
    def __init__(self, print):
        self.__print = print

    def run(self, name, foo="bar"):
        self.__print(f"Hello {name} from plugin default with arg foo: {foo}")


def init(config):
    app = ioc.Plugins.default()
    return app

