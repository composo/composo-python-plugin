from {{ app.name.package }} import ioc


class DefaultPlugin:
    def __init__(self, ui):
        self.__ui = ui

    def run(self, name, foo="bar"):
        self.__ui.print(f"Hello {name} from plugin default with arg foo: {foo}")


def init(config, ui):
    app = ioc.Plugins.default(ui)
    return app

